---
title: maimai DX Top Score Analysis
description: Use McMai to review your highest-rated maimai DX charts, update source data, and access Best 40, AP50, PC50, and friend comparison commands.
---

# maimai DX Top Score Analysis

McMai turns your synchronized play records into a top-score summary and offers related views for Best 40, AP results, play count, and friend comparisons. Before querying, [update your data and select the correct region](/en/guide/score-tracking).

## How to query

1. Update your records using the method for your region.
2. Confirm that CN, JP, or INTL is selected.
3. Send `/b50`; append `#username` to view another player when they allow it.

## Preview

<figure class="mcmai-b50-preview">
  <img src="/images/b50-example.png" alt="Example maimai B50 result image generated by McMai" loading="lazy" decoding="async">
  <figcaption>Thumbnail preview of a B50 result image with player information anonymized.</figcaption>
</figure>

## Best 50

Query your Best 50 scores (Top 35 SD + Top 15 DX).

**Command:** `/b50`

Add `#username` to query another player's data (requires their permission).

**Examples:**

| Command |
|---------|
| `/b50` |
| `/b50 #SomeUser` |

---

## Best 40

Query your Best 40 scores (Top 25 SD + Top 15 DX).

**Command:** `/b40`

Add `#username` to query another player's data (requires their permission).

---

## AP50

Query your top 50 AP / APP scores.

**Command:** `/ap50`

Add `#username` to query another player's data (requires their permission).

---

## PC50

Query your top 50 most-played songs.

::: info CN Only
This feature is only available for CN region users.
:::

**Command:** `/pc50`

Add `#username` to query another player's data (requires their permission).

---

## PK B50

Compare your B50 against another player's, using their B50 songs as the baseline to see score differences on the same charts.

**Command:** `/pk50 [#username]`

- Specify an opponent: `/pk50 #Sakura`
- No argument: randomly match a player from the current group

**Examples:**

| Command | Description |
|---------|-------------|
| `/pk50 #Sakura` | PK against Sakura |
| `/pk50` | Random match in the current group |

---

## Update B50 Data

Sync your latest B50 data from maimai NET to local cache.

**Command:** `/update`

::: tip CN Users — Update via QR Code
CN users can use the "Player QR Code" feature from the 舞萌 | 中二 WeChat official account. Long-press the QR code, copy the `SGWCMAID` string, and send it to the Bot to trigger a CN B50 data update.
:::
