# Best 50

## Best 50

Best 50 成績を照会します（SD Top 35 + DX Top 15）。

**コマンド：** `/b50`

`#ユーザー名` を付けると他のプレイヤーのデータを照会できます（相手の許可が必要）。

**例：**

| コマンド |
|---------|
| `/b50` |
| `/b50 #SomeUser` |

---

## Best 40

Best 40 成績を照会します（SD Top 25 + DX Top 15）。

**コマンド：** `/b40`

`#ユーザー名` を付けると他のプレイヤーのデータを照会できます（相手の許可が必要）。

---

## AP50

AP / APP 成績の上位 50 件を照会します。

**コマンド：** `/ap50`

`#ユーザー名` を付けると他のプレイヤーのデータを照会できます（相手の許可が必要）。

---

## PC50

プレイ回数の多い上位 50 曲を照会します。

::: info CN サーバー限定
この機能は CN サーバーのユーザーのみ利用可能です。
:::

**コマンド：** `/pc50`

`#ユーザー名` を付けると他のプレイヤーのデータを照会できます（相手の許可が必要）。

---

## PK B50

相手の B50 楽曲を基準に、同じ譜面でのスコア差を比較します。

**コマンド：** `/pk50 [#ユーザー名]`

- 相手を指定する場合：`/pk50 #Sakura`
- 指定しない場合：現在のグループ内で同サーバーのプレイヤーとランダムマッチ

**例：**

| コマンド | 説明 |
|---------|------|
| `/pk50 #Sakura` | Sakura と PK |
| `/pk50` | グループ内でランダムマッチ |

---

## B50 データ更新

maimai NET から最新の B50 データをローカルキャッシュに同期します。

**コマンド：** `/データ更新`

::: tip CN サーバー ユーザー — QR コードで更新
CN ユーザーは「舞萌 | 中二」WeChat 公式アカウントの「プレイヤー QR コード」機能を使用します。QR コードを長押しでスキャンし、`SGWCMAID` 文字列をコピーして Bot に送信すると、CN B50 データが自動更新されます。
:::
